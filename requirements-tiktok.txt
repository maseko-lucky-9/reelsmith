# Vendored-library deps for the in-app TikTok cookie adapter.
# NOT included in base requirements.txt — install separately when using
# YTVIDEO_SOCIAL_PROVIDER_TIKTOK=cookie.
#
#   pip install -r requirements-tiktok.txt
#
# Runtime prerequisite (not pip): Node 18+ on PATH.
# Run scripts/tiktok-setup.sh to install the tiktok-signature Node bundle.
fake-useragent>=1.5.0
requests-auth-aws-sigv4>=0.7
requests>=2.32
python-dotenv>=1.0
