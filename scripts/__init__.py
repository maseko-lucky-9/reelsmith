"""Operator scripts for ReelSmith.

This package exists so the smoke gates can be invoked both as modules
(``python -m scripts.ltx_smoke``) and imported from unit tests
(``from scripts import ltx_smoke``) without importing heavy ML deps.
"""
