services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: reelsmith
      POSTGRES_PASSWORD: reelsmith
      POSTGRES_DB: reelsmith
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U reelsmith"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
